import XCTest
@testable import SwiftAPIClientRemote

final class SwiftAPIClientRemoteTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftAPIClientRemote().text, "Hello, World!")
    }
}
