import XCTest
@testable import SwiftAPIClient_Remote

final class SwiftAPIClient_RemoteTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftAPIClient_Remote().text, "Hello, World!")
    }
}
